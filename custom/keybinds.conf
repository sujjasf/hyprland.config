# See https://wiki.hyprland.org/Configuring/Binds/
#!
##! User
bind = Ctrl+Super, Slash, exec, xdg-open ~/.config/illogical-impulse/config.json # Edit shell config
bind = Ctrl+Super+Alt, Slash, exec, xdg-open ~/.config/hypr/custom/keybinds.conf # Edit extra keybinds

# Add stuff here
# Use #! to add an extra column on the cheatsheet
# Use ##! to add a section in that column
# Add a comment after a bind to add a description, like above

##! Sujjal 
bind = Ctrl+Super+Alt, Slash, exec, xdg-open ~/.config/hypr/custom/keybinds.conf # Set KeyBindings 🤓

bind = , XF86Calculator, exec, gnome-calculator # Calculator btn🤓

## Secondary QuickShell overview (search) binding
## Press Ctrl+Alt+Space to open the default QuickShell overview/search as a secondary shortcut.
## Uses the same QuickShell action as the main overview toggle.
# bindd = Ctrl+Alt, Space, App launcher, exec, bash -c 'command -v wofi >/dev/null 2>&1 && exec wofi --show drun || command -v rofi >/dev/null 2>&1 && exec rofi -show drun || (qs -c $qsConfig ipc call TEST_ALIVE && qs -c $qsConfig ipc call overviewToggle)' # Secondary overview
bindd = Ctrl+Alt, Space, App launcher, exec, bash -c 'command -v rofi >/dev/null 2>&1 && exec rofi -show drun || command -v wofi >/dev/null 2>&1 && exec wofi --show drun || (qs -c $qsConfig ipc call TEST_ALIVE && qs -c $qsConfig ipc call overviewToggle)' # Secondary overview
# bindd = Ctrl+Alt, Space, Secondary overview, global, quickshell:overviewToggle

##! Search Thingy
# Placeholder/no-op binds to expose commands in the Hypr cheatsheet
# (use unused keys so they don't interfere with normal workflow)
bind = , F13, exec, true # `/` - Action commands (e.g. `/gameon`) 
bind = , F14, exec, true # `>` - App launcher (fuzzy search)  
bind = , F15, exec, true # `;` - Clipboard history   
bind = , F16, exec, true # `:` - Emoji picker
bind = , F17, exec, true # `=` - Calculator / math expressions
bind = , F18, exec, true # `$` - Run shell commands
bind = , F19, exec, true # `?` - Web search queries

